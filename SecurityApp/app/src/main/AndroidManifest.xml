<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="SecurityApp">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFirstApp">
        <activity
            android:name="com.example.SecurityApp.display_scrabble_lock"
            android:exported="false"
            android:parentActivityName="com.example.SecurityApp.MainActivity"/>
        <activity
            android:name="com.example.SecurityApp.DisplayPatternLock"
            android:exported="false"
            android:parentActivityName="com.example.SecurityApp.MainActivity" />
        <activity
            android:name="com.example.SecurityApp.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>